version: 1
frontend:
  phases:

    preBuild:
      commands:
        - pip install flask
    # IMPORTANT - Please verify your build commands
    build:
      commands:
      - python home.py
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []